services:
  backend:
    container_name: backend-prod

    environment:
      NODE_ENV: production
      PORT: 4000

    ports:
      - "4000:4000"

    volumes:
      - backend-prod-uploads:/usr/src/app/uploads

volumes:
  backend-prod-uploads:
